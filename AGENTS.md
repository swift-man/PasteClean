# PasteClean 에이전트 작업 지침

이 파일은 저장소 전체에 적용됩니다. 더 하위 경로에 별도의 `AGENTS.md`가
생기면 해당 파일의 지침이 그 경로에서 우선합니다.

## 제품 정의

PasteClean은 다음 두 사용 방식을 함께 제공하는 macOS 개발자 도구입니다.

- 앱에서 입력과 결과를 나란히 편집하는 독립 실행형 코드 정리 에디터
- Xcode의 현재 파일 또는 선택 영역을 정리하는 Source Editor Extension

두 진입점은 같은 정리 규칙을 사용해야 합니다. 앱 전용 동작을 정리 엔진에
넣거나 XcodeKit 타입을 공용 도메인 모델로 유출하지 마세요.

PasteClean은 코드 포매터가 아닙니다. 기존 코드의 의미나 구조를 추론해서
다시 작성하지 않고, 붙여넣기 과정에서 생긴 빈 줄·줄 끝 공백·들여쓰기만
보수적으로 정리합니다.

## 현재 책임과 의존 방향

- `PasteCleanExtension/CodeCleaner.swift`
  - Foundation만 사용하는 순수 정리 엔진입니다.
  - SwiftUI, AppKit, XcodeKit 및 영속 저장소에 의존하면 안 됩니다.
- `PasteCleanExtension/CleanPastedCodePlanner.swift`
  - 편집기 독립적인 위치·선택 영역·편집 계획을 계산합니다.
  - 원본 버퍼를 직접 변경하지 않고 먼저 전체 계획을 완성해야 합니다.
- `PasteCleanExtension/CleanPastedCodeCommand.swift`
  - XcodeKit을 도메인 타입으로 변환하는 얇은 어댑터입니다.
  - 정리 규칙이나 선택 영역 계산을 이 파일에 추가하지 마세요.
- `PasteClean/ContentView.swift`
  - 사용자 의도와 화면 상태를 연결하는 SwiftUI 프레젠테이션 계층입니다.
  - 텍스트 정리 알고리즘을 뷰에 구현하지 마세요.
- `PasteClean/CodeTextView.swift`
  - AppKit 텍스트 편집기를 SwiftUI에 연결하는 어댑터입니다.
- `PasteClean/GuideView.swift`, `PasteClean/HelpMenu.swift`
  - 가이드 상태와 macOS 도움말 메뉴 연결을 담당합니다.
- `PasteCleanTests/`
  - Xcode 호스트 없이 순수 엔진과 편집 계획을 검증합니다.

허용되는 기본 의존 방향은 다음과 같습니다.

`SwiftUI/AppKit 또는 XcodeKit 어댑터 → 편집 계획 → 정리 엔진`

반대 방향의 import나 콜백을 만들지 마세요.

## SOLID 설계 원칙

### 단일 책임 원칙(SRP)

- 타입이나 함수에는 변경 이유가 하나만 있어야 합니다.
- 화면 배치, 사용자 설정 저장, 정리 규칙, Xcode 버퍼 변환을 한 타입에
  섞지 마세요.
- 뷰가 복잡해지면 의미 있는 화면 단위로 분리하되, 한 번만 쓰이는 짧은
  표현을 기계적으로 새 타입으로 만들지는 마세요.
- 주석은 코드의 동작을 반복하지 말고 제약, 의도, 보존해야 할 불변식을
  설명하세요.

### 개방-폐쇄 원칙(OCP)

- 새로운 편집기 진입점이나 출력 방식을 추가할 때 기존 정리 알고리즘을
  복제하거나 조건문으로 계속 확장하지 마세요.
- 편집기 경계에서는 작은 값 타입이나 프로토콜을 사용해 변환하고, 공용
  엔진은 기존 호출자를 깨지 않는 조합 가능한 입력을 받도록 설계하세요.
- 새 옵션은 기본값에서 기존 결과가 완전히 유지되어야 합니다.

### 리스코프 치환 원칙(LSP)

- `CleanPastedCodeBuffer` 구현은 줄과 선택 영역의 좌표 의미, UTF-16 열,
  편집 적용 순서를 동일하게 지켜야 합니다.
- 프로토콜 구현에서 지원하지 않는 입력을 조용히 무시하거나, 특정 구현만
  아는 선행 호출을 요구하거나, 범위를 임의로 보정하지 마세요.
- 대체 구현과 테스트 더블은 실제 Xcode 어댑터와 같은 사후 조건을
  만족해야 합니다.

### 인터페이스 분리 원칙(ISP)

- 호출자가 사용하지 않는 기능까지 포함하는 큰 서비스 프로토콜을 만들지
  마세요.
- 읽기, 편집 적용, 선택 영역 갱신처럼 실제 변경 이유가 다를 때만 작은
  인터페이스로 분리하세요.
- 테스트를 위해 모든 구조체와 함수를 프로토콜로 감싸지 마세요. 외부
  프레임워크, 시간, 저장소 또는 변경 가능한 버퍼와의 경계에서만 추상화를
  우선합니다.

### 의존 역전 원칙(DIP)

- 고수준 정리 규칙은 SwiftUI, AppKit, XcodeKit, `UserDefaults` 같은 세부
  구현을 직접 알면 안 됩니다.
- 외부 상태가 필요한 객체는 생성자나 좁은 어댑터로 의존성을 받으세요.
- 전역 공유 상태는 macOS 메뉴처럼 프레임워크 제약상 필요한 경계에만
  제한하고, 도메인 계산에는 사용하지 마세요.

SOLID는 타입 수를 늘리는 목표가 아닙니다. 중복 제거, 명확한 의존 방향,
테스트 가능한 경계를 만드는 데 실질적으로 도움이 될 때 적용하세요.

## 반드시 보존할 동작

- LF, CRLF, CR 및 Unicode 줄바꿈을 처리하고 기존 줄바꿈 방식을 보존합니다.
- 의도한 빈 줄은 남기되 붙여넣기로 생긴 이중 간격과 연속 빈 줄만 줄입니다.
- 줄 끝 공백과 탭을 제거합니다.
- 탭·공백 출력 방식, 탭 너비, 들여쓰기 너비를 1...16 범위에서 처리합니다.
- 연속 줄의 정렬, 여러 줄 문자열, raw 문자열, 정규식 및 주석 내부 내용은
  훼손하지 않습니다.
- 선택 영역이 없으면 전체 파일을 정리하고, 여러 선택 영역과 caret의 순서와
  결과 위치를 보존합니다.
- Xcode 좌표의 column은 UTF-16 기준으로 계산합니다.
- 큰 파일과 다중 선택 처리는 앞부분을 반복 스캔하지 않는 선형 특성을
  유지합니다. 성능 보장은 시간 측정보다 기존 작업량 metric 테스트로
  검증하세요.

동작을 바꾸는 수정에는 정상 사례뿐 아니라 빈 입력, 범위 경계, 혼합
줄바꿈, 문자열·주석 문맥, 다중 선택 회귀 테스트를 함께 추가하세요.

## UI와 다국어

- 앱은 최소 창 크기 `980 × 480`에서도 잘리거나 겹치지 않아야 합니다.
- 입력 관련 설정은 Input에, 결과 작성 방식 관련 설정은 Output에 배치합니다.
- `Indent Using`, `Widths`, `Tab`, `Indent`처럼 Xcode UI와 직접 대응하는
  명칭은 현재 영문 표기를 유지합니다.
- 사용자에게 보이는 새 문구는 `PasteClean/Localizable.xcstrings`에 영어와
  한국어를 모두 제공하세요.
- 링크는 목적지가 문구에서 분명해야 하며, 키보드 및 VoiceOver 접근성을
  유지하세요.
- 표준 macOS 도움말 메뉴는 앱 사용 가이드를 열어야 합니다. 별도의 중복
  사용법 메뉴를 다시 만들지 마세요.
- 앱 사용 가이드는 최초 실제 표시 후 한 번만 자동으로 열려야 하고,
  도움말 메뉴를 통한 수동 열기는 계속 가능해야 합니다.
- 실제 레이아웃 변경은 가능하면 실행한 앱에서 최소 창 크기, 한국어와 영어,
  메뉴·스테퍼·링크의 상호작용을 확인하세요.

## Xcode 프로젝트 변경

- 이 프로젝트는 파일 참조와 Build Phase를 `project.pbxproj`에 명시적으로
  관리합니다. 새 Swift 파일을 만들면 올바른 그룹과 타깃 Sources에 포함됐는지
  확인하세요.
- 앱, 확장 프로그램, 테스트 타깃의 `MARKETING_VERSION`은 `VERSION.txt`와
  일치시킵니다.
- 사용자에게 보이는 기능 변화는 같은 변경에서 `CHANGELOG.md`를 갱신하세요.
- 로컬 사용자 데이터인 `xcuserdata`, `*.xcuserstate`, `.DS_Store`와 빌드
  산출물을 커밋하지 마세요.

## 검증 명령

변경 범위에 맞게 실행하되, 사용자 동작이나 공용 엔진을 바꿨다면 다음을
기본 검증으로 사용합니다.

```bash
jq empty PasteClean/Localizable.xcstrings
git diff --check
xcodebuild test -quiet \
  -project PasteClean.xcodeproj \
  -scheme PasteCleanTests \
  -destination 'platform=macOS' \
  CODE_SIGNING_ALLOWED=NO
xcodebuild build -quiet \
  -project PasteClean.xcodeproj \
  -scheme PasteClean \
  -configuration Release \
  -destination 'platform=macOS' \
  CODE_SIGNING_ALLOWED=NO
```

- 테스트 실패를 기존 문제라고 추정하지 말고 현재 브랜치와 기준 브랜치에서
  재현 여부를 확인하세요.
- 실제 Xcode Extension 등록, Editor 메뉴 노출, undo 동작은 단위 테스트로
  대체할 수 없으므로 관련 변경 시 Xcode에서 수동 확인 항목으로 남기세요.
- 코드리뷰 범위와 제외 대상은 `.reviewbot.yml`을 따릅니다. 특히 빌드 산출물,
  생성 소스, 바이너리 리소스, 로컬 Xcode 메타데이터, `Sample/**`은 리뷰에서
  제외합니다.

## Git, 커밋 및 PR

- 사용자가 요청하지 않은 커밋, push, PR 생성, merge는 하지 마세요.
- 기존 로컬 변경은 사용자의 작업으로 간주하고 보존하며, 관련 없는 변경을
  되돌리거나 함께 포맷하지 마세요.
- 커밋과 PR 제목은 변경 유형에 맞춰 `feat.`, `fix.`, `docs.`, `perf.`,
  `chore.`, `hotfix.`, `env.` 같은 접두사를 사용하고 한국어로 자세히
  작성하세요.
- 제목이나 본문에 `[Codex]`, `[codex]`를 사용하지 마세요.
- PR은 Draft가 아닌 Ready 상태로 만드세요.
- PR을 merge한 뒤 원격과 로컬 작업 브랜치를 삭제해 정리하세요.
- force push, hard reset, 사용자 변경 삭제처럼 복구하기 어려운 작업은 명시적
  승인 없이 실행하지 마세요.

사용자가 “코드리뷰 검토”라고 요청하면 다음 순서를 따릅니다.

1. 열린 PR의 모든 리뷰 댓글과 대댓글, 미해결 스레드, CI 상태를 확인합니다.
2. 각 지적을 실제 코드와 재현 결과로 검증하여 수정 필요 또는 리뷰어의
   오판·환각으로 분류하고, 해당 스레드에 판단 근거를 댓글로 남깁니다.
3. 수정이 필요하면 로컬에서 수정·검증·push한 뒤 merge하지 않고 리뷰를
   기다립니다.
4. 수정할 문제가 없으면 PR을 merge하고 브랜치를 삭제합니다.
5. merge한 경우 PR의 전체 댓글에서 도출되는 의미 있는 후속 개선을 검토하고,
   범위가 분리되는 실질적 개선이 있을 때만 별도 Ready PR로 진행합니다.
